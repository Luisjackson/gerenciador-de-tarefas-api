# Arquitetura do Sistema

O sistema utiliza uma arquitetura em camadas:

Apresentação  →  Serviço  →  Model  → Persistencia  →  Banco de dados.

Controller → Service → Model  → Repository → Banco de Dados

Cada camada possui responsabilidades definidas, evitando acoplamento indevido.